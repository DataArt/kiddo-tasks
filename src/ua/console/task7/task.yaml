sceneType: console
winButton:
  url: ./task8
  text: Next level
initialScript: |
  import console

  # Додати код тут
  
taskDescription: Розділи значення змінної "a" на значення змінної "b" та запиши залишок від цього ділення у змінну "c".
generatingFunc: >-
  this.addConsoleVariable('a', Math.floor(Math.random() * Math.floor(8)));
  this.addConsoleVariable('b', Math.floor(Math.random() * Math.floor(6)));
  this.addConsoleVariable('c');
  this.addCheckingLogic(' return this.getVariableValue("c") === this.getVariableValue("a") % this.getVariableValue("b") ? null : "TASK_DONE_WRONG" ');
